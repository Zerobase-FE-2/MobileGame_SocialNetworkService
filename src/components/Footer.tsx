import styled from 'styled-components';

const FooterDiv = styled.div`
  position: absolute;
  bottom: auto;
  width: 100vw;
  height: 12rem;
  background-color: #42afff;
`;

export const Footer = () => {
  return <FooterDiv />;
};
